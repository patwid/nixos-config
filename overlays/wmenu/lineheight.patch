diff --git a/menu.c b/menu.c
index f95409f..6b7ceb6 100644
--- a/menu.c
+++ b/menu.c
@@ -161,7 +161,7 @@ void menu_getopts(struct menu *menu, int argc, char *argv[]) {
 	}
 
 	int height = get_font_height(menu->font);
-	menu->line_height = height + 3;
+	menu->line_height = height + 4;
 	menu->height = menu->line_height;
 	if (menu->lines > 0) {
 		menu->height += menu->height * menu->lines;
